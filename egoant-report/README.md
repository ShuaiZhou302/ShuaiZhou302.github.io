# EgoANT × HomER blog-style report

Self-contained ablation note for HomER 25 / 470.

```bash
python3 serve_report.py --port 8765
```

Reading order: background (incl. GEPA) → metrics → contact sheet → walkthrough → cost (raw vs selector) → ablations → recipe → English prompts.
